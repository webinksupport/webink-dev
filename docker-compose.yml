version: '3.8'
services:
  webink-dev:
    build: .
    container_name: webink-dev
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - HOSTNAME=0.0.0.0
